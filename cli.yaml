# General information
name: Aries Data Generator Toolkit
version: "0.1"
author: Animo Solutions
about: A toolkit to interact with an Aries instance for quick data manipulation
settings:
    - ArgRequiredElseHelp

# Subcommands
subcommands: 
  - start:
      about: initialize
      version: "0.1"
      args:
        # A config file with all the data
        - config:
            short: c
            long: config
            value_name: FILE
            help: Sets a custom config file
            takes_value: true

